---
title: Blog
body_class: blog
---

%article
  %section.section
    - blog.published_articles[0...20].each_with_index do |article, index|
      %div.row
        %div.columns.small-12.large-10.large-offset-2
          %div.content-layer.default-content-paddings
            .meta
              %span.year= article.date.strftime('%Y')
              %span.month= article.date.strftime('%h')
              %span.day= article.date.strftime('%d')
              %span.dayname= article.date.strftime('%a')
            %h3= link_to article.title, article.url_without_extension
            %ul.tags
              - article.tags.each do |tag|
                %li
                  %span.round.alert.label
                    = tag
            .block
              = article.summary
              %p.more
                = link_to "read on&hellip;", article.url_without_extension
